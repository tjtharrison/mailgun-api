{
  "name": "mailgun-api",
  "version": "1.0.0",
  "description": "API server to send emails via Mailgun",
  "main": "app.js",
  "bin": {
    "mailgun-api": "bin/mailgun.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "mailgun-js": "^0.22.0"
  }
}
